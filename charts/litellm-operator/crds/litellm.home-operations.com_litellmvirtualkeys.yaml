---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: litellmvirtualkeys.litellm.home-operations.com
spec:
  group: litellm.home-operations.com
  names:
    kind: LiteLLMVirtualKey
    listKind: LiteLLMVirtualKeyList
    plural: litellmvirtualkeys
    shortNames:
    - llkey
    singular: litellmvirtualkey
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.proxyRef
      name: Proxy
      type: string
    - jsonPath: .spec.secretName
      name: Secret
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LiteLLMVirtualKey is a virtual key generated and deleted through
          LiteLLM's admin API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LiteLLMVirtualKeySpec defines a virtual key managed through
              a LiteLLMProxy.
            properties:
              aliases:
                additionalProperties:
                  type: string
                description: Aliases maps model aliases to their target model names.
                type: object
              budgetDuration:
                description: BudgetDuration resets the budget after this duration,
                  such as "30d".
                type: string
              duration:
                description: Duration is how long the key remains valid, such as "30d".
                type: string
              keyAlias:
                description: KeyAlias is a human-readable LiteLLM alias for this key.
                type: string
              maxBudget:
                description: MaxBudget is the maximum spend allowed for this key as
                  a decimal string.
                type: string
              maxParallelRequests:
                description: MaxParallelRequests caps concurrent requests made with
                  this key.
                format: int64
                type: integer
              metadata:
                additionalProperties:
                  type: string
                description: Metadata stores additional string metadata on the LiteLLM
                  key.
                type: object
              models:
                description: Models lists the models this key may call. An empty list
                  permits all models.
                items:
                  type: string
                type: array
                x-kubernetes-list-type: atomic
              proxyRef:
                description: ProxyRef is the LiteLLMProxy whose admin API creates
                  and deletes this key.
                type: string
              rpmLimit:
                description: RPMLimit caps requests per minute for this key.
                format: int64
                type: integer
              secretKey:
                default: key
                description: SecretKey is the key in SecretName containing the generated
                  virtual key.
                type: string
              secretName:
                description: SecretName is the name of the Secret managed by the operator
                  containing the generated key.
                type: string
              teamID:
                description: TeamID associates this key with a LiteLLM team.
                type: string
              tpmLimit:
                description: TPMLimit caps tokens per minute for this key.
                format: int64
                type: integer
              userID:
                description: UserID associates this key with a LiteLLM user.
                type: string
            required:
            - proxyRef
            - secretName
            type: object
          status:
            description: LiteLLMVirtualKeyStatus reports the observed state of the
              virtual key.
            properties:
              conditions:
                description: Conditions represent the latest observations of the virtual
                  key's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
