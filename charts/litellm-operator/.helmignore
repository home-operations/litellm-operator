.DS_Store
.git/
.gitignore
*.tmp
*.bak
*.orig
.vscode/
.idea/
# helm-docs input template; the generated README.md ships, this doesn't.
README.md.gotmpl
# helm-unittest specs (run in CI, not shipped).
/tests/
